import 'package:flutter/material.dart';

class UserRegisterPolicyAgreement extends StatelessWidget {
  const UserRegisterPolicyAgreement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('User register policy agreement view'),
      ),
    );
  }
}
